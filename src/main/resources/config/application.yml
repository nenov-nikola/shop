spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    properties:
        org.hibernate.dialect.MySQLDialect: update
  server.contextPath: /shop-0.0.1-SNAPSHOT
